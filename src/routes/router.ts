import express from 'express';
const router = express.Router();

//Controller
// import { getAllUsers, getUsersByName, getUsersByType, getUsersOrderedByNameOrType, getAllUsersPaginate } from '../controllers/UsersController';

//Rotas usuários
// router.get("/user", getAllUsers);
// router.get("/user/:name", getUsersByName);
// router.get("/users/:type", getUsersByType);
// router.get("/users-order", getUsersOrderedByNameOrType);
// router.get("/users-paginate", getAllUsersPaginate);

export default router;
